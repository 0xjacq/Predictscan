# Predict.fun API Configuration
# Optional: Mainnet API key. If not provided, the scanner operates in Testnet mode.
PREDICT_API_KEY=your_mainnet_api_key_here

# Local Server Settings
PORT=3000
DEFAULT_NETWORK=mainnet

# Rate limit and cache settings (scan interval in seconds)
CACHE_TTL=5
